<?php

/**
 * Конфигурация генерации данных SpringGalaxy (web_spring/).
 * Подключается генератором web_spring/generate_systems.php.
 *
 * Лоция систем пока генерируется случайно; в будущем будет браться
 * из текстовых описаний, а не генерироваться.
 */

return [
    'generation' => [
        // кол-во планет в системе; с шансом belt_chance вместо 5-й планеты — пояс астероидов
        'planets_min' => 2,
        'planets_max' => 6,

        // 0..1 — вероятность пояса астероидов на орбите 5-й планеты
        'belt_chance' => 0.5,

        // генерировать ли облако Оорта (пояс астероидов в районе «Плутона») для каждой системы
        'generate_oort' => true,

        // АЕ — орбита гиперканалов (район орбиты 6-й планеты)
        'jump_radius_au' => 9.54,

        // АЕ — радиус облака Оорта (район «Плутона»)
        'oort_radius_au' => 39.5,

        // null — каждый запуск случайный; число — детерминированная генерация
        'seed' => null,
    ],
];
